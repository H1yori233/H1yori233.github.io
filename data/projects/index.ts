import diceThrone from './dice-throne'
import kidTalk from './kidtalk'
import popUpMidi from './pop-up-midi'

// 导出所有项目为数组
export const projects = [diceThrone, kidTalk, popUpMidi]

// 单独导出每个项目，方便单独引用
export { diceThrone, kidTalk, popUpMidi } 